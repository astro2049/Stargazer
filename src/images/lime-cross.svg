<svg xmlns="http://www.w3.org/2000/svg"
     width="90" height="90"
     fill="#c2fe0c" opacity="0.3"
>
    <!-- Shift the origin to the center -->
    <g transform="translate(45, 45)">
        <!-- Vertical bar -->
        <rect x="-0.5" y="-15" width="1" height="30"/>
        <!-- Horizontal bar -->
        <rect x="-15" y="-0.5" width="30" height="1"/>
    </g>
</svg>
